import 'package:flutter/material.dart';

class SelectDoctorCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Select Doctor Category'),
      ),
      body: Center(
        child: Text('Select Doctor Category Page'),
      ),
    );
  }
}
