import 'package:flutter/material.dart';

class CallAmbulancePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Call Ambulance'),
      ),
      body: Center(
        child: Text('Call Ambulance Page'),
      ),
    );
  }
}
