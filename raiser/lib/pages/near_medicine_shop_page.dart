import 'package:flutter/material.dart';

class NearMedicineShopPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Near Medicine Shop'),
      ),
      body: Center(
        child: Text('Near Medicine Shop Page'),
      ),
    );
  }
}
