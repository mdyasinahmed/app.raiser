import 'package:flutter/material.dart';

class HospitalLocationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hospital Location'),
      ),
      body: Center(
        child: Text('Hospital Location Page'),
      ),
    );
  }
}
